# Step C rollback review R1

The 0.5 s route-less candidate produced a recording and all robot-efficiency
metrics identical to the accepted baseline, so it failed the required material
improvement gate. This rollback removes only the delay constant and restores
the prior 3 s predicates. Accepted provenance changes remain intact.

Verdict: PASS
