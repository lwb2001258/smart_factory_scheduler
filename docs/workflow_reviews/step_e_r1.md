# Step E dynamic reservation grant review R1

Step D recording proves most fleet-wide emergencies are nominal reservation
deadlines, not spatial collision reactions. The fix does not delete the
deadline: after it expires, motion is permitted only by a fresh grant matching
the active epoch and current waypoint. Supervisor issues that grant only after
the existing measured-pose swept-segment, peer, turning-zone and terminal
validator succeeds. With no grant, wrong identity or expired grant, the old
zero-speed emergency path is unchanged.

Verdict: PASS
