# ????????? Workflow

> ?????????????????????????????????????????????????????
> ???????????????? code review????????? Webots ????????????? Scenario C?FCFS?seed 42 ? 1200s Webots ???

## ????

- ??????????????????????????????
- ?????????? `waypoints[current_waypoint_idx:]`?
- ????????????????????????????
- ?? DWA/sidestep ????????????????????? `navigate`?????????
- ???????????????????? 90? ?????????????????????

## ?????

### Step 1?????????????
- `WaypointNavigator.set_waypoints()` ?? `_reset_reactive_state()`?
- ? `navigate` ?? sidestep?emergency?replan ???
- ?? sidestep ??? `replan_requested`????????????

### Step 2?joint ????????
- `_joint_collision_scan()` ?? `_joint_predictive_speed_shield()` ???????
- ???????????? `_joint_liveness_needed=True`?????? `_next_joint_grid_tick`?
- ????? `JointGridPlanner` ???????????????

### Step 3?fallback ???????
- `_build_joint_fallback_plans()` ??????? `grid_planner.plan()`?
- ???????? `plan_grid_lifelong()`?????????????????

### Step 4???????
- joint watchdog ? 3 ??????? fresh all-active plan?
- 5 ???????? `_joint_escalate_stalled_robots()`????????
- legacy ???????? 10 ???? 6 ??

### Step 5????? 90? ??
- `_joint_head_on_yield()` ??????????????????
- ???????????????????????? 90? ??????????
- ???????? peer ?? 0.80m ?????? `_segment_clear` ???
- ??????????? fleet joint replan????????????????
- ?????????????????????? 0.70?????????? 0.40 ????? 1.0?

## Code Review ??

?? Step ???? review?
1. ????`git diff --check`?`python -m py_compile ...`?`python -m pytest -q`?
2. ???????????route-write ????????????????? peer ???????

## Webots ????

???????????????????? 180s?

```powershell
$env:SMART_FACTORY_SIM_DURATION='180'
$env:SMART_FACTORY_AUTO_STOP='1'
$env:SMART_FACTORY_ENABLE_JOINT_RUNTIME='1'
$env:SMART_FACTORY_ENABLE_RHCR='0'
python scripts/run_experiments.py --scenario C --scheduler FCFS --seeds 1 --seed-values 42 --webots 'C:\Program Files\Webots\msys64\mingw64in\webots.exe'
```

???????? 1200s?

```powershell
$env:SMART_FACTORY_SIM_DURATION='1200'
$env:SMART_FACTORY_AUTO_STOP='1'
$env:SMART_FACTORY_ENABLE_JOINT_RUNTIME='1'
$env:SMART_FACTORY_ENABLE_RHCR='0'
python scripts/run_experiments.py --scenario C --scheduler FCFS --seeds 1 --seed-values 42 --webots 'C:\Program Files\Webots\msys64\mingw64in\webots.exe'
```

?????
- Webots ??????????? `results/experiment_C_FCFS_*.json`?
- ????? `stalled`/`deadlock`/?? `replan` ???
- ?????????????
- ????????? `_joint_head_on_yield` / `_joint_escape_robot` ??????????????
