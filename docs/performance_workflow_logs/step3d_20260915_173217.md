# Step 3d 验收记录

- Step：`Step3d_priority_yield_direct_cooldown`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：在 `_priority_yield_direct_plan()` 入口复用 `priority_yield_cooldown_until`，阻止同一 yielder 在 2.0s 内重复执行 direct-yield replan。

## 修改

- `controllers/factory_supervisor/factory_supervisor.py`
  - `_priority_yield_direct_plan()` 在取得 yielder 后、任何规划前增加：
    `if getattr(robot, 'priority_yield_cooldown_until', 0.0) > self.sim_time: return False`
  - 不改变 winner/yielder 选择、goal 计算、路径规划、clearance 校验或 route-write 事务。
  - 复用既有冷却状态，不新增协议字段。

## 本地验证

- `python -m py_compile controllers/factory_supervisor/factory_supervisor.py`：通过。
- `python -m pytest tests/test_supervisor_prediction.py tests/test_motion_coordination.py tests/test_priority_yield_resume.py tests/test_metrics_safety.py -q`：`196 passed, 4 subtests passed`。

## Code Review R1

- 结论：`PASS`
- 是否只影响性能，不改变业务决策：是。首次 direct-yield 仍立即执行，只抑制 2.0s 内重复触发。
- 调度、路径、安全判断是否保持相同：是，所有规划与安全阈值未改。
- 是否有隐藏 API、状态机或协议变化：无，复用现有 `priority_yield_cooldown_until`。

## Code Review R2

- 结论：`PASS`
- 是否新增同步阻塞、热路径 I/O、无限重试或锁竞争：无，O(1) 浮点比较。
- 是否影响安全距离、碰撞避免、事务提交/回滚：无，距离阈值与事务顺序未改；direct-yield 失败后仍走既有 head-on/escape/fresh-plan 后备路径。
- 是否便于回滚和复现：是，仅一处条件。

## Webots 录屏

- 动画：`results/performance_workflow/step3d/webots_diagnostic_C_FCFS_20260915_172843.html`
- 结果：`results/performance_workflow/step3d/experiment_C_FCFS_20260915_172843.json`
- 停车分析：`results/performance_workflow/step3d/stop_analysis.json`
- 性能摘要：`results/performance_workflow/step3d/performance_summary.json`
- 运行配置：`results/performance_workflow/step3d/run_config.json`

## 门禁结果

| 指标 | Step1 基线 | Step3d | 判定 |
|---|---:|---:|---|
| wall_seconds | 130.7 | 97.3 | 更快 |
| sim_to_wall_ratio | 1.3772 | 1.8499 | 更快 |
| route_dispatches | 330 | 237 | 明显减少 |
| audited_replans | 34 | 23 | 减少 |
| unplanned_task_stops | 5 | 1 | 减少 |
| min_pair_distance | 0.5477 | 0.5700 | 不退化 |
| pair_distance_violations | 0 | 0 | PASS |
| abnormal_mass_stop_seconds | 40.128 | 33.600 | 下降 |
| rotating_in_place_intervals | 46 | 42 | 下降 |
| unexplained_active_intervals | 61 | 75 | FAIL，上升 |
| clear_motion_duty_cycle | 0.92236 | 0.91127 | FAIL，下降 |
| unblocked_zero_speed_ratio | 0.00728 | 0.05454 | FAIL，上升 |

事件变化：

- `_priority_yield_direct`：`route/replan` 均从 12 降至 5。
- `_command_reverse`：`route/replan` 均从 17 降至 14。
- `joint_grid_transaction`：296 降至 214。

## 结论

该单变量显著减少重复 direct-yield 和整体 route dispatch，但机器人运行效率主门禁回退：
`unexplained_active_intervals` 上升、`clear_motion_duty_cycle` 下降、`unblocked_zero_speed_ratio` 上升，说明抑制 2.0s 窗口过宽，造成更多无法解释的走走停停。按 workflow 最高优先级规则判定 `FAILED` 并回滚。

## 回滚

- 已移除 `_priority_yield_direct_plan()` 中的冷却提前返回。
- 回滚后复测：`196 passed, 4 subtests passed`。
- 回滚后代码恢复为 Step1 观测基线。
