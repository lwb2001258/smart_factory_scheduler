# Step 3f 验收记录

- Step：`Step3f_joint_refresh_min_interval_3s`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：将普通滚动联合规划的刷新下限从 2.0s 提高到 3.0s。

## 修改

- `controllers/factory_supervisor/config.py`
  - 新增 `JOINT_REFRESH_MIN_INTERVAL = 3.0`。
- `controllers/factory_supervisor/factory_supervisor.py`
  - `_joint_refresh_interval()` 在存在活动 waypoint offsets 时使用 3.0s 下限。
  - 空 offsets 分支保持 2.0s，避免改变无活动前缀的初始化节奏。
- `tests/test_supervisor_prediction.py`
  - 同步更新受影响的刷新间隔期望值。

## 本地验证

- `python -m py_compile controllers/factory_supervisor/config.py controllers/factory_supervisor/factory_supervisor.py`：通过。
- `python -m pytest tests/test_supervisor_prediction.py tests/test_motion_coordination.py tests/test_priority_yield_resume.py tests/test_metrics_safety.py -q`：`196 passed, 4 subtests passed`。

## Code Review R1

- 结论：`PASS`
- 是否只影响性能，不改变业务决策：是。仅调整 ordinary refresh 的触发节奏；规划算法、优先级、安全判断均未变。
- 调度、路径、安全判断是否保持相同：是，达到刷新条件后仍使用原规划器和事务流程。
- 是否有隐藏 API、状态机或协议变化：无，仅新增配置常量并更新对应单测。

## Code Review R2

- 结论：`PASS`
- 是否新增同步阻塞、热路径 I/O、无限重试或锁竞争：无，仅改变时间阈值。
- 是否影响安全距离、碰撞避免、事务提交/回滚：无，安全驱动/liveness/冲突刷新不受此下限限制。
- 是否便于回滚和复现：是，仅配置常量和一处阈值。

## Webots 录屏

- 动画：`results/performance_workflow/step3f/webots_diagnostic_C_FCFS_20260915_173759.html`
- 结果：`results/performance_workflow/step3f/experiment_C_FCFS_20260915_173759.json`
- 停车分析：`results/performance_workflow/step3f/stop_analysis.json`
- 性能摘要：`results/performance_workflow/step3f/performance_summary.json`
- 运行配置：`results/performance_workflow/step3f/run_config.json`

## 门禁结果

| 指标 | Step1 基线 | Step3f | 判定 |
|---|---:|---:|---|
| wall_seconds | 130.7 | 138.6 | 未改善 |
| sim_to_wall_ratio | 1.3772 | 1.2987 | 未改善 |
| route_dispatches | 330 | 330 | 未变化 |
| audited_replans | 34 | 34 | 未变化 |
| abnormal_mass_stop_seconds | 40.128 | 40.128 | 未改善 |
| unexplained_active_intervals | 61 | 61 | 未改善 |
| rotating_in_place_intervals | 46 | 46 | 未改善 |
| joint_equivalent_refreshes_suppressed | 0 | 0 | 未触发 |
| clear_motion_duty_cycle | 0.92236 | 0.92236 | 未改善 |

事件明细与基线完全一致：`joint_grid_transaction=296`，`_command_reverse=17`，
`_priority_yield_direct=12`，`_joint_stall_recovery=5`。

## 结论

本场景 296 次 joint refresh 主要由 liveness/conflict/route-less 触发，而不是普通滚动 cadence；仅提高普通刷新下限不会减少路由下发，运行效率主门禁没有改善。按 workflow 规则判定 `FAILED` 并回滚。

## 回滚

- 已移除 `JOINT_REFRESH_MIN_INTERVAL`，恢复 `_joint_refresh_interval()` 原 2.0s 下限。
- 已恢复原测试期望值。
- 回滚后复测：`196 passed, 4 subtests passed`。
- 回滚后代码恢复为 Step1 观测基线。
