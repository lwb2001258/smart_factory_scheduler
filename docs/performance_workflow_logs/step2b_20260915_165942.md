# Step 2b 验收记录

- Step：`Step2b_segment_distance_lru`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：给 `JointGridPlanner._segment_distance` 增加有界 `lru_cache(maxsize=16384)`。

## 修改

- `controllers/factory_supervisor/joint_grid_planner.py`
  - 增加 `from functools import lru_cache`。
  - `_segment_distance` 加 `@lru_cache(maxsize=16384)`。
  - 距离计算本身未改变。

## 本地验证

- `py_compile`：通过。
- `pytest tests/test_joint_grid_planner.py tests/test_motion_coordination.py`：
  `27 passed, 4 subtests passed`。
- microprofile：八机器人难例 50 次从约 `0.467s` 降至 `0.260s`，
  cache hits `44844`，misses `906`。

## R1 / R2

- R1：`PASS`，纯函数缓存，输出与未缓存完全一致。
- R2：`PASS`，缓存有界，无新 I/O、锁竞争、事务或安全影响。

## Webots 录屏

- 动画：`results/performance_workflow/step2b/webots_diagnostic_C_FCFS_20260915_165942.html`
- 确认录屏：`results/performance_workflow/step2b_confirm/webots_diagnostic_C_FCFS_20260915_170251.html`
- 结果：`results/performance_workflow/step2b/experiment_C_FCFS_20260915_165942.json`
- 停车分析：`results/performance_workflow/step2b/stop_analysis.json`

## 门禁结果

| 指标 | Step1 | Step2b | Step2b_confirm | 判定 |
|---|---:|---:|---:|---|
| wall_seconds | 130.7 | 135.6 | 143.8 | 未稳定优于基线 |
| joint_planning_p50_ms | 102.49 | 73.62 | - | 改善 |
| joint_planning_p95_ms | 177.93 | 131.63 | - | 改善 |
| joint_planning_p99_ms | 195.36 | 159.92 | - | 改善 |
| abnormal_mass_stop_seconds | 40.128 | 40.128 | 40.128 | 未改善 |
| unexplained_active_intervals | 61 | 61 | 61 | 未改善 |

- 安全/业务/停车指标仍一致。
- 运行效率主门禁没有改善：`abnormal_mass_stop_seconds` 和
  `unexplained_active_intervals` 不变，录屏仍未消除规律性停顿。
- 因此按新 workflow 的最高优先级规则判定 FAILED，并回滚。

## 回滚

- 已移除 `lru_cache` 导入和装饰器，恢复原 `_segment_distance`。
- 回滚后单测通过：`27 passed, 4 subtests passed`。

## 结论

`_segment_distance` 缓存能显著降低联合规划器 CPU 耗时，但本场景的机器人
“走一步停一下”主指标未改善，不能作为有效优化保留。
