# Step 1 验收记录

- Step：`Step1_observability`
- 日期：2026-09-15
- 状态：`PASS`
- 单变量：仅增加分阶段观测埋点，不改变调度、规划、安全、事务/回滚决策。

## 修改清单

- `controllers/factory_supervisor/metrics_collector.py`
  - 新增 `supervisor_webots_step_wall_ms`
  - 新增 `phase_wall_ms`
  - 新增 `joint_planning_tier_wall_ms`
  - 新增 `robot_dwa_wall_ms`
  - 新增对应 `record_*` 方法和最终 JSON 汇总。
- `controllers/factory_supervisor/motion_coordinator.py`
  - 新增 `metrics` 引用。
  - 新增 `_record_joint_tier_wall()`。
  - 在 wide/primary/short/soft 四层规划调用处记录耗时。
- `controllers/factory_supervisor/factory_supervisor.py`
  - 记录 `Supervisor.step()` 自身耗时。
  - 记录 pose_read、broadcast、receive、dispatch、robot_state_update、
    dynamic_safety_shield、joint_runtime_watchdog、reservation_refresh、
    joint_planning_phase、task_assignment、metrics_record 阶段耗时。
  - 仅插入 `perf_counter()` 和 `record_phase_wall()`，未移动/修改任何业务调用。
- `controllers/robot_controller/robot_controller.py`
  - 新增 `time` 导入。
  - DWA 内记录最后执行耗时，并在 status telemetry 中上报
    `dwa_control_active` / `dwa_control_wall_ms`。

## 本地验证

- `python -m py_compile ...`：通过。
- `python -m pytest tests/test_metrics_safety.py tests/test_motion_coordination.py tests/test_joint_grid_planner.py tests/test_robot_protocol.py -q`：
  `92 passed, 4 subtests passed`。

## Code Review R1

- 结论：`PASS`
- 是否只影响性能观测：是。
- 调度、路径、安全判断是否保持相同：是，所有原调用顺序和分支条件未变。
- 是否有隐藏 API、状态机或协议变化：无；仅新增可选 status 字段和 metrics 字段。

## Code Review R2

- 结论：`PASS`
- 是否新增同步阻塞、热路径 I/O、无限重试或锁竞争：无。
  - `perf_counter()` 和列表 append 为内存观测；无文件/网络 I/O。
  - DWA 观测通过已有每 10 步 status 上报，不新增发送频率。
- 是否影响安全距离、碰撞避免、事务提交/回滚：无。
- 是否便于回滚和复现：是，改动为独立新增字段/记录，可单独回退。

## Webots 录屏

- 动画 JSON：`results/performance_workflow/step1/webots_diagnostic_C_FCFS_20260915_164234.json`
- 动画 HTML：`results/performance_workflow/step1/webots_diagnostic_C_FCFS_20260915_164234.html`
- X3D：`results/performance_workflow/step1/webots_diagnostic_C_FCFS_20260915_164234.x3d`
- 结果 JSON：`results/performance_workflow/step1/experiment_C_FCFS_20260915_164234.json`
- 停车分析：`results/performance_workflow/step1/stop_analysis.json`
- 性能摘要：`results/performance_workflow/step1/performance_summary.json`

## 性能门禁对比

| 指标 | Step0 基线 | Step1 观测 | 判定 |
|---|---:|---:|---|
| sim_to_wall_ratio | 1.372 | 1.3772 | PASS，无回退 |
| supervisor_step_p50_ms | 4.9424 | 5.0995 | PASS，波动范围内 |
| supervisor_step_p95_ms | 10.2982 | 10.7690 | PASS |
| supervisor_step_p99_ms | 316.0642 | 314.1822 | PASS |
| supervisor_step_max_ms | 1548.02 | 1606.93 | PASS，无新形态冻结 |
| joint_planning_p50_ms | 102.2181 | 102.4878 | PASS |
| joint_planning_p95_ms | 172.4004 | 177.9266 | PASS |
| joint_planning_p99_ms | 193.6327 | 195.3590 | PASS |
| joint_planning_max_ms | 205.7706 | 220.0325 | PASS，run noise |

业务/安全指标全部一致：

- `tasks_completed=1`，`tasks_generated=18`
- `route_dispatches=330`
- `rapid_route_overrides=42`
- `audited_replans=34`
- `unplanned_task_stops=5`
- `min_pair_distance=0.5477`
- `pair_distance_violations=0`
- 停车分析完全一致：`stop_intervals=95`，`abnormal_mass_stop_seconds=40.128`

## Step 1 观测结论

- `Supervisor.step()` 自身中位仅 `3.39ms`，说明主要卡顿不在 Webots 同步步进。
- Python 主循环中联合规划阶段是最大热点：
  - `joint_planning_phase p50=262.02ms`，`p99=505.41ms`，`max=809.58ms`。
- 联合规划内部 primary tier 是主要开销：
  - `primary p50=45.78ms`，`p99=107.75ms`，`max=118.27ms`；
  - soft tier 次高，说明候选常落到较后层。
- `joint_runtime_watchdog` 存在长尾：`p99=127.59ms`，`max=1170.67ms`。
- 本场景 DWA 观测样本为 0，说明 C/FCFS/seed44 在采样步未进入 DWA 分支；
  该字段保留供后续 DWA 专项验证使用。

## 结论

仅观测埋点未改变业务行为，Webots 模拟性能与安全指标未回退。
Step 1 `PASS`，可进入 Step 2 的 DWA 等价加速或先处理联合规划热点。
