# Step 3e 验收记录

- Step：`Step3e_priority_yield_direct_0.5s_dedupe`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：为 `_priority_yield_direct_plan()` 增加同一 `(winner, yielder, goal)` 在 0.5s 内的严格去重。

## 修改

- `controllers/factory_supervisor/config.py`
  - 新增 `PRIORITY_YIELD_DIRECT_DEDUPE_SECONDS = 0.5`。
- `controllers/factory_supervisor/factory_supervisor.py`
  - `RobotInfo` 新增 `_priority_yield_direct_last_key` / `_priority_yield_direct_last_at`。
  - `_priority_yield_direct_plan()` 仅在成功下发后记录去重键和时间；0.5s 内相同键直接返回 `False`。
  - 不改变 winner/yielder 选择、goal、路径规划或安全阈值。

## 本地验证

- `python -m py_compile controllers/factory_supervisor/config.py controllers/factory_supervisor/factory_supervisor.py`：通过。
- `python -m pytest tests/test_supervisor_prediction.py tests/test_motion_coordination.py tests/test_priority_yield_resume.py tests/test_metrics_safety.py -q`：`196 passed, 4 subtests passed`。

## Code Review R1

- 结论：`PASS`
- 是否只影响性能，不改变业务决策：是。仅抑制 0.5s 内同一 yielder/goal 的立即重复 direct-yield。
- 调度、路径、安全判断是否保持相同：是，规划与校验逻辑未改。
- 是否有隐藏 API、状态机或协议变化：无，仅新增两个内部去重字段。

## Code Review R2

- 结论：`PASS`
- 是否新增同步阻塞、热路径 I/O、无限重试或锁竞争：无，O(1) 键比较。
- 是否影响安全距离、碰撞避免、事务提交/回滚：无，安全距离未改，首次 direct-yield 仍可执行。
- 是否便于回滚和复现：是，仅两文件、单变量。

## Webots 录屏

- 动画：`results/performance_workflow/step3e/webots_diagnostic_C_FCFS_20260915_173324.html`
- 结果：`results/performance_workflow/step3e/experiment_C_FCFS_20260915_173324.json`
- 停车分析：`results/performance_workflow/step3e/stop_analysis.json`
- 性能摘要：`results/performance_workflow/step3e/performance_summary.json`
- 运行配置：`results/performance_workflow/step3e/run_config.json`

## 门禁结果

| 指标 | Step1 基线 | Step3e | 判定 |
|---|---:|---:|---|
| wall_seconds | 130.7 | 76.3 | 更快 |
| sim_to_wall_ratio | 1.3772 | 2.3591 | 更快 |
| route_dispatches | 330 | 270 | 减少 |
| audited_replans | 34 | 5 | 大幅减少 |
| unplanned_task_stops | 5 | 3 | 减少 |
| min_pair_distance | 0.5477 | 0.6985 | 不退化 |
| pair_distance_violations | 0 | 0 | PASS |
| abnormal_mass_stop_seconds | 40.128 | 38.592 | 下降 |
| unexplained_active_intervals | 61 | 72 | FAIL，上升 |
| rotating_in_place_intervals | 46 | 59 | FAIL，上升 |
| clear_motion_duty_cycle | 0.92236 | 0.98971 | 上升 |
| unblocked_zero_speed_ratio | 0.00728 | 0.00025 | 下降 |

事件变化：

- `_priority_yield_direct`：`route/replan` 均从 12 降至 1。
- `_command_reverse`：`route/replan` 均从 17 降至 2。
- `joint_grid_transaction`：296 降至 265。

## 结论

0.5s 严格去重对 CPU、整体 route dispatch 和 motion continuity 有显著改善，但机器人运行效率主门禁仍失败：
`unexplained_active_intervals` 上升 11，`rotating_in_place_intervals` 上升 13。录屏显示机器人更多时间在转向/原地调整，而不是被 replan 引导前进，仍不符合“防止走一步停一下”的最高目标。按 workflow 规则判定 `FAILED` 并回滚。

## 回滚

- 已移除 `PRIORITY_YIELD_DIRECT_DEDUPE_SECONDS` 及 `_priority_yield_direct_plan()` 去重逻辑。
- 回滚后复测：`196 passed, 4 subtests passed`。
- 回滚后代码恢复为 Step1 观测基线。
