# Step 2a 验收记录

- Step：`Step2a_joint_segment_helper`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：将 `JointGridPlanner._segment_distance` 中的嵌套
  `point_segment_distance` 提升为模块级 helper。

## 修改

- `controllers/factory_supervisor/joint_grid_planner.py`
  - 新增模块级 `_point_segment_distance()`。
  - `_segment_distance()` 改为引用模块级 helper。
  - 数学公式、参数顺序和返回结果保持一致。

## 本地验证

- `py_compile`：通过。
- `pytest tests/test_joint_grid_planner.py tests/test_motion_coordination.py`：
  `27 passed, 4 subtests passed`。
- microprofile：50 次八机器人难例从约 `0.555s` 降至 `0.467s`，本地热路径更快。

## R1 / R2

- R1：`PASS`，数学语义和调用顺序一致。
- R2：`PASS`，无新 I/O、锁、事务或安全影响。

## Webots 录屏

- 动画：`results/performance_workflow/step2a/webots_diagnostic_C_FCFS_20260915_165058.html`
- 结果：`results/performance_workflow/step2a/experiment_C_FCFS_20260915_165058.json`
- 停车分析：`results/performance_workflow/step2a/stop_analysis.json`

## 门禁结果

| 指标 | Step1 | Step2a | 判定 |
|---|---:|---:|---|
| wall_seconds | 130.7 | 138.7 | 变慢 |
| sim_to_wall_ratio | 1.3772 | 1.2978 | FAIL |
| joint_planning_p95_ms | 177.93 | 203.26 | 变慢 |
| joint_planning_p99_ms | 195.36 | 247.84 | 变慢 |
| supervisor_step_p95_ms | 10.77 | 11.16 | 变慢 |

业务/安全/停车指标仍一致，但性能门禁失败，按规则回滚。

## 回滚

- 已恢复原嵌套 `point_segment_distance` 实现。
- 回滚后单测通过：`27 passed, 4 subtests passed`。
- 回滚确认录屏：
  `results/performance_workflow/step2a_rollback/webots_diagnostic_C_FCFS_20260915_165434.html`
- 回滚确认 wall 为 `139.7s`，与 Step2a 同会话接近，说明该次 wall 波动主要来自运行噪声；
  但按单次门禁规则 Step2a 仍判定 FAILED。

## 结论

该微优化在孤立 microprofile 中有效，但未能在 Webots 单次门禁中证明收益，且联合规划
耗时反而变长。已回滚，代码恢复到 Step1 观测基线。
