# Step 3c 验收记录

- Step：`Step3c_command_reverse_dedup`
- 日期：2026-09-15
- 状态：`FAILED`（已回滚）
- 单变量：为 `_command_reverse` 增加同一机器人+同一 reverse 距离的 2.0s 去重冷却。

## 修改

- `controllers/factory_supervisor/config.py`
  - 新增 `REVERSE_RECOVERY_COOLDOWN = 2.0`。
- `controllers/factory_supervisor/factory_supervisor.py`
  - `RobotInfo` 新增 `_command_reverse_last_key` / `_command_reverse_last_at`。
  - `_command_reverse()` 在已成功下发同类 reverse 的 2.0s 内直接返回 `False`。
  - 仅在 `_install_runtime_plan()` 成功且 `_last_plan_dispatch_performed` 后记录冷却状态。

## 本地验证

- `python -m py_compile controllers/factory_supervisor/config.py controllers/factory_supervisor/factory_supervisor.py`：通过。
- `python -m pytest tests/test_supervisor_prediction.py tests/test_motion_coordination.py tests/test_priority_yield_resume.py tests/test_metrics_safety.py -q`：`196 passed, 4 subtests passed`。

## Code Review R1

- 结论：`PASS`
- 是否只影响性能，不改变业务决策：是。首次 reverse 恢复仍可立即执行，只抑制成功下发后的短窗重复调用。
- 调度、路径、安全判断是否保持相同：是，候选生成、clearance 阈值、路径验证和安装流程未变。
- 是否有隐藏 API、状态机或协议变化：无，仅新增两个内部记忆字段。

## Code Review R2

- 结论：`PASS`
- 是否新增同步阻塞、热路径 I/O、无限重试或锁竞争：无，纯内存字段比较。
- 是否影响安全距离、碰撞避免、事务提交/回滚：无，安全阈值和事务顺序未改。
- 是否便于回滚和复现：是，仅两文件、单变量。

## Webots 录屏

- 动画：`results/performance_workflow/step3c/webots_diagnostic_C_FCFS_20260915_171935.html`
- 结果：`results/performance_workflow/step3c/experiment_C_FCFS_20260915_171935.json`
- 停车分析：`results/performance_workflow/step3c/stop_analysis.json`
- 性能摘要：`results/performance_workflow/step3c/performance_summary.json`
- 运行配置：`results/performance_workflow/step3c/run_config.json`

## 门禁结果

| 指标 | Step1 基线 | Step3c | 判定 |
|---|---:|---:|---|
| wall_seconds | 130.7 | 125.9 | PASS，无回退 |
| sim_to_wall_ratio | 1.3772 | 1.4297 | PASS，无回退 |
| supervisor_step_p50_ms | 5.0995 | 4.8933 | PASS，无回退 |
| joint_planning_p50_ms | 102.4878 | 93.2314 | PASS，无回退 |
| abnormal_mass_stop_seconds | 40.128 | 40.128 | 未改善 |
| unexplained_active_intervals | 61 | 61 | 未改善 |
| rotating_in_place_intervals | 46 | 46 | 未改善 |
| unplanned_task_stops | 5 | 5 | 未改善 |

事件明细与本场景基线的恢复事件数量完全一致：

- `route_dispatch_events` 330，其中 `joint_grid_transaction=296`，`_command_reverse=17`。
- `replan_events` 中 `_command_reverse=17`。
- `_command_reverse` 同类重复调用在本 180s 场景中均间隔大于 2.0s，冷却未触发。

## 结论

单变量在本地测试和 Webots 中均无回归，但机器人运行效率主门禁指标完全不变，未达到“提高运行效率、减少走一步停一下”的目标。按 workflow 规则判定 `FAILED` 并回滚。

## 回滚

- 已移除 `REVERSE_RECOVERY_COOLDOWN` 及 `_command_reverse` 冷却逻辑。
- 回滚后复测：`196 passed, 4 subtests passed`。
- 回滚代码恢复为 Step1 观测基线；Step3c 录屏本身与 Step1 的停车/事件指标完全一致，可作为回滚等价证据。
