# 项目栅格尺寸与可用性判定说明

## 1. 结论概览

项目中存在两套用途不同的网格，不能混为一谈：

| 网格 | 用途 | 单格尺寸 | 是否决定机器人能否通行 |
|---|---|---:|---|
| 路径规划占用栅格 `OccupancyGrid` | A*、联合路径规划、避障 | **0.25 m × 0.25 m** | **是** |
| RL 拥堵统计网格 | 强化学习状态中的拥堵分布 | **1 m × 1 m** | 否 |
| Webots 地板贴图格 | 场景地面的视觉分块 | **1 m × 1 m** | 否 |

因此，如果问题是“机器人怎样判断某个格能不能走”，应查看 `grid_planner.py` 中分辨率为 `0.25` 米的占用栅格，而不是 `config.py` 中值为 `1.0` 的拥堵网格，也不是 Webots 中的地板砖尺寸。

路径规划栅格覆盖的工厂坐标范围为：

- X 方向：`[-9.0, 9.0]` 米，名义宽度 18 米；
- Y 方向：`[-5.5, 5.5]` 米，名义高度 11 米；
- 列数：`18 / 0.25 = 72`；
- 行数：`11 / 0.25 = 44`；
- 总格数：`72 × 44 = 3168`。

当前默认配置实际构建出的统计结果为：

| 状态 | 格数 | 含义 |
|---|---:|---|
| `CELL_FREE` | 1350 | 普通可通行格 |
| `CELL_OBSTACLE` | 663 | 实体障碍物或边界格，不可通行 |
| `CELL_INFLATED` | 1155 | 障碍物安全膨胀区，不可通行 |
| `CELL_AVOID` | 0（初始状态） | 可通行但代价较高的动态避让格 |

初始普通自由格比例约为 `42.61%`。`CELL_AVOID` 是规划期间临时设置的状态，所以刚初始化时通常为 0。

## 2. 一个路径规划格有多大

路径规划分辨率由以下常量决定：

```python
GRID_RES = 0.25
```

所以一个格在真实世界中代表：

```text
0.25 m × 0.25 m = 25 cm × 25 cm
```

相邻横向或纵向格中心相距 0.25 米；相邻对角格中心相距：

```text
sqrt(0.25² + 0.25²) ≈ 0.3536 m
```

机器人碰撞半径为 `ROBOT_RADIUS = 0.18 m`，直径约为 0.36 米。因此机器人本体直径约占 `0.36 / 0.25 = 1.44` 个格。规划器不是只把机器人当成一个无尺寸的点，而是通过障碍物膨胀给机器人本体和安全距离留出空间。

## 3. 世界坐标和格坐标怎样转换

世界坐标 `(x, y)` 转格坐标 `(col, row)`：

```python
col = round((x - (-9.0)) / 0.25)
row = round((y - (-5.5)) / 0.25)
```

格坐标转回世界坐标：

```python
x = -9.0 + col * 0.25
y = -5.5 + row * 0.25
```

例如世界坐标 `(0, 0)`：

```text
col = round(9 / 0.25) = 36
row = round(5.5 / 0.25) = 22
```

对应格坐标 `(36, 22)`。

实现使用 `round()` 将位置映射到最近的格中心。有效数组下标是列 `0..71`、行 `0..43`；是否越界最终由 `in_bounds()` 判断，而不能只根据名义工厂范围判断。

## 4. 四种格子状态

占用栅格使用整数保存每一格的状态：

```python
CELL_FREE     = 0
CELL_OBSTACLE = 1
CELL_INFLATED = 2
CELL_AVOID    = 3
```

### 4.1 `CELL_FREE`：普通可用格

这是默认状态。一个格不在实体障碍、工厂边界和障碍膨胀范围内时，保持为 `CELL_FREE`。

其通行代价为 `1.0`，A* 可以正常经过。

### 4.2 `CELL_OBSTACLE`：绝对不可用格

下列位置会被标成实体障碍格：

1. 四个货架占据的格；
2. 六张工作站桌子占据的格；
3. `SHELF_GAP_BOXES` 定义的策略禁行区（当前配置为空）；
4. 靠近工厂边缘的边界环。

障碍物几何来自 `config.py` 的 `OBSTACLE_BOXES`。该列表中的半宽、半高已经加过机器人半径，`OccupancyGrid._build_obstacles()` 在栅格化时会先减去 `ROBOT_RADIUS`，恢复实体的原始尺寸，然后根据每个格中心是否落入矩形来标记：

```text
abs(cell_x - obstacle_center_x) <= raw_half_width
且
abs(cell_y - obstacle_center_y) <= raw_half_height
```

工厂边界另有 `boundary_pad = 0.30 m`。格中心距任一名义边缘不足 0.30 米时，也会被标为 `CELL_OBSTACLE`，避免机器人伸出场地或撞墙。

### 4.3 `CELL_INFLATED`：安全膨胀形成的不可用格

实体障碍标记完成后，程序会把障碍周围进一步设为不可通行。膨胀距离为：

```text
ROBOT_RADIUS + SAFETY_MARGIN
= 0.18 m + 0.55 m
= 0.73 m
```

换算成格数：

```text
ceil(0.73 / 0.25) = 3 格
```

程序以每个 `CELL_OBSTACLE` 为中心，把列、行方向偏移均不超过 3 格的自由格标成 `CELL_INFLATED`。这里使用的是 Chebyshev 距离，所以形成的是较保守的方形膨胀区域，而不是精确圆形区域。

需要注意：3 格的离散膨胀对应格中心最多偏移 0.75 米。这一距离已经包含机器人半径，因此可以把机器人中心当成规划点，同时仍给机器人外轮廓留下安全余量。

`CELL_INFLATED` 和实体障碍一样，普通规划时不可经过；区别只是前者代表安全缓冲区，后者代表真实物体或硬边界。

### 4.4 `CELL_AVOID`：可用但不推荐的格

`CELL_AVOID` 仍然是可通行格，但其代价是：

```python
DYNAMIC_AVOID_COST = 8.0
```

即经过该格的成本是普通自由格的 8 倍。A* 会优先寻找其他路线；只有绕行困难或没有更合适的低代价路线时才会穿过。

当前代码主要在处理其他机器人的路径时临时使用它：先尝试把对方近期路径硬标为 `CELL_OBSTACLE`；如果这样导致无路可走，再把相关自由格临时标为 `CELL_AVOID`，进行软避让规划；规划结束后恢复原状态。

## 5. 最终怎样判断“可用”或“不可用”

核心入口是 `OccupancyGrid.is_free(col, row)`：

```python
if not self.in_bounds(col, row):
    return False
return self.cells[row][col] not in (CELL_OBSTACLE, CELL_INFLATED)
```

因此最终规则非常明确：

| 情况 | `is_free()` 结果 | A* 能否经过 |
|---|---|---|
| 越界 | `False` | 不能 |
| `CELL_OBSTACLE` | `False` | 不能 |
| `CELL_INFLATED` | `False` | 不能 |
| `CELL_FREE` | `True` | 能，代价 1 |
| `CELL_AVOID` | `True` | 能，代价 8，尽量避开 |

可以把判定过程概括为：

```text
候选位置
  ↓ 世界坐标转格坐标
是否在 72 × 44 范围内？ ── 否 ─→ 不可用
  │是
  ↓
是否为实体障碍 CELL_OBSTACLE？ ── 是 ─→ 不可用
  │否
  ↓
是否为膨胀区 CELL_INFLATED？ ── 是 ─→ 不可用
  │否
  ↓
CELL_FREE → 正常可用
CELL_AVOID → 可用，但高代价
```

## 6. A* 使用格子时的附加规则

### 6.1 支持八方向移动

A* 可以向上、下、左、右和四个对角方向移动。横纵移动基础成本为 1，对角移动基础成本为 `sqrt(2)`。

### 6.2 禁止沿障碍角落斜穿

即使目标对角格可用，如果构成对角的两个相邻横纵格中任意一个不可用，也禁止这次对角移动。这样能防止机器人从两个障碍的角之间“切角”穿过。

### 6.3 直线路径会重复检查

路径平滑和路径抽样不是直接连接远处的两个点，而是每隔 `GRID_RES / 2 = 0.125 m` 沿线采样，逐个调用 `is_free()`。此外还会用配置层的矩形线段相交检查再次验证，避免平滑后的线段穿过实体障碍。

## 7. 起点和终点附近的特殊例外

工作站、储存点和充电点可能紧邻桌子、货架或边界，因此其目标格可能落入 `CELL_INFLATED`。如果完全按普通规则处理，机器人可能永远无法到达停靠点。

调用 `GridAStar.plan()` 时，默认会临时放宽端点附近的膨胀格：

- 普通起点/终点：放宽半径 2 格，即最多约 0.5 米的格偏移；
- 充电站端点：放宽半径 4 格，即最多约 1.0 米的格偏移；
- 只允许把 `CELL_INFLATED` 临时改成 `CELL_FREE`；
- 真实的 `CELL_OBSTACLE` 永远不会被放宽；
- 规划完成或失败后都会恢复原来的 `CELL_INFLATED` 状态。

因此，“膨胀格不可用”是常规规则，但起终点附近存在受控、临时的例外；实体障碍格始终不可用。

如果端点仍不可用，规划器还会在最多 8 格的半径内寻找最近的自由格，即最多搜索约 2 米的格偏移范围。

## 8. 动态机器人占用如何影响可用性

静态占用栅格主要描述货架、桌子和边界。其他机器人及其近期路径在具体规划调用中动态加入：

1. 收集其他机器人的已预留格和近期路径；
2. 对近期路径中心线扩展安全带；
3. 临时把安全带内的普通自由格改成 `CELL_OBSTACLE`；
4. 先尝试规划一条完全避开的路径；
5. 如果硬阻塞导致无路可走，则恢复后改用 `CELL_AVOID` 高代价软避让；
6. 每次规划结束后恢复格子的原状态。

所以某格是否“当前可用”不仅取决于启动时构建的静态地图，也可能受其他机器人的即时路线影响。时间维度上的格子冲突和机器人间最小距离还会由联合路径规划器继续检查；静态 `is_free()` 为真并不等于该格在任意时刻都可以立刻被任意机器人占用。

## 9. 另外两种 1 米网格

### 9.1 RL 拥堵网格

`config.py` 中还有：

```python
GRID_RESOLUTION = 1.0
GRID_WIDTH = 20
GRID_HEIGHT = 16
```

它是强化学习状态空间中的拥堵图，单格为 1 米，用来表达区域中机器人的密集程度。它不是 A* 的障碍地图，也不直接决定某位置能否通过。

### 9.2 Webots 地板视觉格

`worlds/smart_factory.wbt` 中：

```text
floorSize 20 16
floorTileSize 1 1
```

这表示场景地板为 20 米 × 16 米、视觉地砖为 1 米 × 1 米。它描述的是仿真场景地板的外观和尺寸，不是规划器内部的 0.25 米占用栅格。

## 10. 关键代码位置

- `controllers/factory_supervisor/grid_planner.py`
  - 工厂边界和 `GRID_RES = 0.25`；
  - 四种格状态；
  - 坐标转换与 `is_free()`；
  - 静态障碍栅格化和安全膨胀；
  - A*、端点放宽、对角防切角和路径平滑。
- `controllers/factory_supervisor/config.py`
  - `ROBOT_RADIUS = 0.18`；
  - 货架、工作站桌子的 `OBSTACLE_BOXES`；
  - RL 拥堵网格的 1 米分辨率。
- `controllers/factory_supervisor/motion_coordinator.py`
  - 其他机器人路径的临时硬阻塞和 `CELL_AVOID` 软避让。
- `controllers/factory_supervisor/joint_grid_planner.py`
  - 多机器人联合时空路径中的格可通行性、距离和冲突检查。
- `worlds/smart_factory.wbt`
  - Webots 场景的地板尺寸和 1 米视觉地砖。

